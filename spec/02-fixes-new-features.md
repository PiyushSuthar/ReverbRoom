UI and features are good. We need to implement the following fixes and new features:

- A page showing the saved files with options to play, delete, and share them.
- A settings page to adjust audio quality, reverb parameters, and other preferences.
- Noise reduction feature to improve recording quality in noisy environments.

Fix the following issues:

- The padding above the header is too large, causing the header to be pushed down. Reduce the padding to make the UI more compact.
