we need to add options like room size, width, damp for reverb. and similarly beats, decay for echo.

- we need an option to save the current settings as a preset, so that users can easily recall their favorite configurations.
- we need to add a feature to load presets, allowing users to quickly switch between different configurations without manually adjusting each parameter.
- better noise reduction algorithms should be implemented to improve audio quality, especially in live recording scenarios.
- we need to add a feature for real-time audio visualization, allowing users to see the effects of their adjustments on the audio waveform or spectrum. (trivial but useful)

Chore:

- we need a pipeline that automatically builds the apk and attaches it to the release page on GitHub, so that users can easily download the latest version of the app without having to manually build it themselves. Use github actions for this task.
- A bash script to automatically version the app and release a new version on GitHub, including updating the changelog and tagging the release. This will streamline the release process and ensure consistency across versions.

Make sure to follow the best practices for code quality, including writing unit tests for new features and ensuring that the codebase remains maintainable and well-documented. Also follow and update versioning guidelines to ensure that users can easily track changes and updates to the app.
